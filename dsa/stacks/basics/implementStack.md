we can use an array to add the elements with a stack pointer which basically keeps a track of the last inserted element
and no random access is allowed

else i will have a pop method where i will return the element which teh stack pointer is pointing to and move the pointer a single index behind

